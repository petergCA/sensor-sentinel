{
  "config": {
    "step": {
      "user": {
        "title": "Sensor Sentinel",
        "description": "Set up the performance-safe unavailable-entity watchdog. You can tune exclusion rules afterwards from the Configure button."
      }
    },
    "abort": {
      "already_configured": "Sensor Sentinel is already set up."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Exclusion rules & tuning",
        "description": "Rules over lists. Anything matched here is never reported as down.",
        "data": {
          "bad_states": "States treated as \"bad\"",
          "excluded_domains": "Excluded domains",
          "excluded_integrations": "Excluded integrations",
          "excluded_patterns": "Excluded entity_id globs (e.g. *_firmware, roborock_*)",
          "excluded_entities": "Excluded entities",
          "grace_period": "Grace window (seconds a flap is ignored)",
          "integration_threshold": "Roll up as one incident at N entities per integration",
          "notify_targets": "Mobile notify services (e.g. mobile_app_iphone)",
          "persistent_notification": "Create persistent notifications"
        },
        "data_description": {
          "bad_states": "Which entity states count as \"down\". An entity in one of these states — past the grace window and not excluded — becomes a reported incident. Default: unavailable + unknown.",
          "excluded_domains": "Whole domains to never report (e.g. button, scene). Pick from the domains present in your system, or type one.",
          "excluded_integrations": "Whole integrations to never report. Every entity from a selected integration is fully excluded. The list is the integrations currently in your system; you can also type one that isn't loaded yet.",
          "excluded_patterns": "Glob patterns matched against entity_id, e.g. *_firmware or roborock_*.",
          "grace_period": "How long an entity must stay bad before it's reported, so brief flaps don't page you.",
          "integration_threshold": "When at least this many entities from one integration are down at once, report them as a single rolled-up incident. 0 disables the roll-up.",
          "notify_targets": "notify.* service names without the notify. prefix, e.g. mobile_app_your_phone."
        }
      },
      "preview": {
        "title": "Dry-run preview",
        "description": "These rules would silence {count} currently-down entit(y/ies):\n\n{preview}\n\nSubmit to save, or go back to adjust."
      }
    }
  }
}
